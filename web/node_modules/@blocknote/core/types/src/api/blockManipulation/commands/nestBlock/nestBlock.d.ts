import { NodeType } from "prosemirror-model";
import { Transaction } from "prosemirror-state";
import { BlockNoteEditor } from "../../../../editor/BlockNoteEditor.js";
export declare function nestBlock(editor: BlockNoteEditor<any, any, any>): boolean;
/**
 * Modified version of prosemirror-schema-list's liftListItem.
 * https://github.com/ProseMirror/prosemirror-schema-list/blob/master/src/schema-list.ts
 *
 * Changes from the original:
 * 1. Range predicate checks node.type instead of firstChild.type (same as sinkItem)
 * 2. Passes groupType to liftToOuterList
 * 3. Operates on Transaction directly instead of state+dispatch
 * 4. Skips liftOutOfList (root-level blocks can't be unnested in BlockNote)
 */
export declare function liftItem(tr: Transaction, itemType: NodeType, groupType: NodeType): boolean;
export declare function unnestBlock(editor: BlockNoteEditor<any, any, any>): boolean;
export declare function canNestBlock(editor: BlockNoteEditor<any, any, any>): boolean;
export declare function canUnnestBlock(editor: BlockNoteEditor<any, any, any>): boolean;
